/**
 * Application lifecycle listeners.
 *
 * <p>The listener package contains startup logic that prepares runtime state
 * shared by controllers, including loading persisted deadline values into
 * {@code ServletContext} and configuring text-file store paths for the deployed
 * web application.</p>
 */
package listener;

/**
 * Text-file persistence stores.
 *
 * <p>Store classes are responsible for reading and writing data under
 * {@code WEB-INF/file}. They handle CSV escaping, tab/Base64 application form
 * records, deadline files, and runtime file-path resolution for Tomcat
 * deployment.</p>
 */
package store;

/**
 * AI and resume-text implementation classes.
 *
 * <p>Implementations include Groq API clients, mock fallback generators, and
 * PDF resume extractors. The fallback classes keep the application usable when
 * no API key is configured or when an AI request fails during a demo.</p>
 */
package service.ai.impl;

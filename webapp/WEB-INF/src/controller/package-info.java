/**
 * Servlet controllers for the TA recruitment web application.
 *
 * <p>Controller classes receive HTTP requests, check sessions and role access,
 * prepare request attributes, and forward to JSP views. Business rules are kept
 * in service classes so that controller methods remain focused on web flow,
 * redirects, and request/response handling.</p>
 */
package controller;
